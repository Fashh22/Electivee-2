-- Complete Quiz System Database Setup
-- Run this in phpMyAdmin: New Server → SQL tab → Paste → Go

CREATE DATABASE IF NOT EXISTS `quiz_data` CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE `quiz_data`;

-- [Paste the entire content of quiz_data-3.sql here after the USE statement]

-- Tables: answers, notifications, questions, quizzes, results, users, verification_codes
-- Sample data: 2 quizzes (Filipino, Science), users, results included

-- After running: Check Tables tab for 7 tables loaded successfully.
